services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: marmot

    volumes:
      - db_data:/var/lib/postgresql/marmot/data 

volumes:
  db_data:
